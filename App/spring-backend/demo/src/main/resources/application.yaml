spring:
  application:
    name: demo
  datasource:
    #url: jdbc:postgresql://localhost:5432/deltaspacedb3
    url: jdbc:postgresql://172.31.46.238:5432/deltaspacedb #toggle this before ./build.ps1
    username: postgres
    #password: ${POSTGRES-PASSWORD}
    password: user # toggle this before ./build.ps1
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB


aws:
  s3:
    bucket-name: reddit-clone-deltaspace-eu
    region: eu-central-1
  credentials:
    access-key: ${AWS_ACCESS_KEY}
    secret-key: ${AWS_SECRET_KEY}
